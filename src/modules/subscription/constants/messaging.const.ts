export const SUBSCRIPTION_RELEASE_QUEUE = 'subscription.release.queue';

export const SUBSCRIPTION_RELEASE_RETRY_QUEUE = 'subscription.release.retry.queue';

export const RETRY_TIME_IN_MS = {
  SUBSCRIPTION_RELEASE: 10_000,
} as const;
